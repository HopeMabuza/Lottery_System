// TODO: Add general utility helpers

export function classNames(...classes) {
  // Placeholder: merge class names
  return classes.filter(Boolean).join(' ');
}

export function shortenAddress(address) {
  // Placeholder: shorten a wallet address for display
  if (!address) return '';
  return address;
}
